%ただ単にmatlabを把握するためのファイル
clc; clear; close all;
x=magic(5);
x
disp(x);
x(2,1:3)
%% section
%セクションの後の%%の後は半角スペース
a=zeros(3,3);
a
b=ones(3)
d=ones(1,3)

%% セクション次
A=magic(6)
A([1 5 6],:)

%% セクション
A=[1,3;2,2];
B=[1,1;1,1];
A*B
D=B*inv(A)
C=B/A
A*inv(A)

%% 行列演算と転置
A=[1,2,3;3,2,1];
A
A'
b=[1;2;1]

b'

%% 内積
a=[1;2;1];
b=[4;3;-5];
c=dot(a,b)
d=a'*b

%% 配列の処理
a=[3 2 6];
a=[a 1]
b=[2 5]
c=[a b]
c=c*3-1
c>=10

%% zeros
clc; clear; close all;